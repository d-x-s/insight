export class IdValidator {

	constructor() {
		console.log("Constructor for IdValidator");
	}

	// NB: a promise may be required for this function
	// returning to this later, for now use stub
	// NB: If promise required, function cannot be static
	// check if id conforms to EBNF standards
	public checkId(id: string): boolean {
		// validate

		// create
		return true;
	}

}
